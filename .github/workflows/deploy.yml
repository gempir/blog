name: Blog Deploy

on:
  push:
    branches:
    - master

jobs:
    deploy:
        name: deploy to gh-pages
        runs-on: ubuntu-latest
        steps:
        - uses: master
        - name: deploy
          run: |
              ./deploy.sh
